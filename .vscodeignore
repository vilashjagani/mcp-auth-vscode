# source — not needed at runtime
src/**
tsconfig.json
esbuild.js

# IDE / tool state
.claude/**

# dev tooling
node_modules/**
.vscode-test/**
.github/**
out/**/*.map

# repo artefacts
.gitignore
CONFIGURATION.md
*.vsix

# large media — hosted on GitHub, not bundled in the vsix
media/*.gif
